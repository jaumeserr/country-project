import Select from "components/Select";
import Input from "components/Input";

const Filters = () => {
  return (
    <div>
      <Select />
      <Input />
    </div>
  );
};

export default Filters;
