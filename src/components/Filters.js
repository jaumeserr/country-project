import Select from "components/Select";
import Search from "components/InputSearch";

const Filters = () => {
  return (
    <div>
      <Select />
      <Search />
    </div>
  );
};

export default Filters;
